[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "visual-map-localizer"
version = "0.2.0"
description = "Single-image 6DoF Visual Positioning System on top of COLMAP / hloc"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
    { name = "Ryohei Sasaki" }
]
keywords = ["SfM", "COLMAP", "VPS", "localization", "SLAM", "computer-vision"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: POSIX :: Linux",
    "Topic :: Scientific/Engineering :: Image Recognition",
]
dependencies = [
    # Lightweight runtime — sufficient for unit tests, JSON I/O, PnP, and
    # Sim(3) helpers. Heavy deep-learning deps live under the `[deep]`
    # extra; see README.
    "numpy>=1.23",
    "scipy>=1.10",
    "opencv-python>=4.7",
    "Pillow>=9.5",
    "tqdm>=4.65",
    "h5py>=3.8",
    "pyyaml>=6.0",
    "pycolmap>=0.6.1",
    "click>=8.1",
    "rich>=13.0",
]

[project.scripts]
visual-map-localizer = "visual_map_localizer.cli.main:cli"

[project.optional-dependencies]
# Required only for the real retrieval / matching pipeline (NetVLAD, DISK,
# LightGlue). `hloc` is not on PyPI, so after `pip install .[deep]` you
# also need:
#     pip install git+https://github.com/cvg/Hierarchical-Localization.git@master
deep = [
    "torch>=2.0",
    "torchvision>=0.15",
]
# Required only to regenerate `docs/assets/demo.gif` via
# `python scripts/render_demo.py`. Plain users do not need this.
demo = [
    "matplotlib>=3.7",
    "imageio>=2.30",
]
dev = [
    "pytest>=7.4",
    "pytest-cov>=4.1",
    "ruff>=0.1",
]

[tool.setuptools.packages.find]
include = ["visual_map_localizer*"]
exclude = ["tests*", "examples*", "docs*", "scripts*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
# ros2/.../test contains a couple of pure-Python helper tests that
# guard themselves with `pytest.importorskip("rclpy")` for the
# ROS-touching cases, so it is safe to collect them even when CI
# does not have rclpy / sensor_msgs available.
testpaths = ["tests", "ros2/visual_map_localizer_ros/test"]
# launch_testing (ROS) hijacks directory-level test collection if it is
# installed system-wide. Disable it together with its ROS companion plugin
# so pytest discovers our normal test_*.py files.
addopts = """-q
  -p no:launch_testing
  -p no:launch_ros
"""
